#ifndef HEROSHADOW_H
#define HEROSHADOW_H

#include "hero.h"

class HeroShadow :public Hero
{
public:
    HeroShadow(int role, int id, const QString &name, int x, int y, int healthPercentage, int magicPercentage);
    ~HeroShadow();

    void moveToPoint(int x, int y);
    void shadowMagic(int withSword, int x, int y);
    void shadowInjure(int leftPercentage);


};

#endif // HEROSHADOW_H
