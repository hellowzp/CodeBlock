#ifndef HEROSHADOW_H
#define HEROSHADOW_H

#include <QGraphicsPixmapItem>
#include "world.h"
#include "statusbar.h"

class HeroShadow :public QGraphicsPixmapItem, public Tile
{
public:
    HeroShadow(int role, int id, int x, int y, int healthPercentage, int magicPercentage);
    void moveToPoint(int x, int y);
    int getId();

private :
    int _id;
    int _role;

    StatusBar *_healthBar;
    StatusBar *_magicBar;
};

#endif // HEROSHADOW_H
