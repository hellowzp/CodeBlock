#include "mainframe.h"
#include <iostream>
#include <QMouseEvent>
#include <QLabel>
#include <QMimeData>
#include <QDrag>
#include <QPainter>
#include <QDragMoveEvent>
#include <QGraphicsDropShadowEffect>

MainFrame::MainFrame(QWidget *parent) :
    QFrame(parent)
{

    this->setAcceptDrops(true);
}


void MainFrame::mousePressEvent(QMouseEvent * event) {

//    QLabel *child = dynamic_cast<QLabel *>(this->childAt(event->pos()));
//    if (child) {
//        QDrag *drag = new QDrag(this);
//        drag->setObjectName("Health package drag");

//        QByteArray itemData;
//        QDataStream dataStream(&itemData, QIODevice::WriteOnly);
//        dataStream << QString("Health package drag");
//        QMimeData *mimeData = new QMimeData;
//        mimeData->setData("application/single_string", itemData);

//        drag->setMimeData(mimeData);

//        QLabel *label = this->findChild<QLabel *>("Health package label");
//        QGraphicsOpacityEffect *effect = new QGraphicsOpacityEffect;
//        effect->setOpacity(0.3);
//        label->setGraphicsEffect(effect);

//        drag->exec();

//    }
}
