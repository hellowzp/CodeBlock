#ifndef GAMEBUIDER_H
#define GAMEBUIDER_H


#include "worldview.h"
#include "QPoint"

class GameBuider
{
public:
    GameBuider();
    void buildGameForLevel(int level);
    static GameBuider *Instance();
    void SetWorldView(WorldView *view);

private :
    //private methods
    QPoint randomWorldPosition();

    WorldView *_targetWorldView;
    static GameBuider *_instance;
};

#endif // GAMEBUIDER_H
